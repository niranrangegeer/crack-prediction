# 裂纹标注指南

## 目标

从 Abaqus 仿真结果里导出裂纹路径图，文件名和 Geom/Sener 图一一对应，
放进 `Crack_Plots/` 文件夹，然后改代码接入训练。

---

## 第一步：在 Abaqus 里跑出裂纹

在仿真里加裂纹扩展模型，有两种方法，选一种：

### 方法 A：XFEM（推荐，不用改网格）

1. 打开你的 `.cae` 文件，进入 **Interaction** 模块
2. 菜单：Special → Crack → Create
3. 类型选 **XFEM**，选择裂纹区域（整个部件或局部区域都行）
4. 在 **Step** 模块里，确认 Step 类型是 Static/General 或 Dynamic/Explicit
5. 在 **Load** 模块里加载（拉伸、压缩等）
6. 提交 Job，跑完后进 **Visualization** 模块
7. 场变量选 **STATUSXFEM**
   - 值 = 0：单元完好
   - 值 = 1：单元完全断裂（这就是裂纹位置）

### 方法 B：内聚力单元（Cohesive Zone，适合界面裂纹）

1. 在可能开裂的界面处插入 cohesive 单元（厚度设为 0）
2. 材料属性里定义 Traction-Separation 法则（设置强度和断裂能）
3. 跑完后场变量选 **SDEG**（损伤变量）
   - 值 = 0：完好
   - 值 = 1：完全断裂

---

## 第二步：导出裂纹图

这一步最容易出错，以下几点必须和 Geom/Sener 图保持完全一致：

- **视角**：View → Views → 和之前导出 Geom/Sener 时完全相同的视角
- **切片位置**：Section 编号要一一对应，slice_1 对 slice_1，不能错位
- **图片尺寸**：建议 512×512 或和 Geom/Sener 相同
- **不要有多余内容**：关掉坐标轴、图例、标题栏，只保留场的颜色图
  - View → Show/Hide → 把 Legend、Title、Compass 全关掉

### 手动导出单张图

File → Print → 选 PNG，保存到对应的 `Crack_Plots/` 文件夹，
文件名格式：`slice_1_crack.png`、`slice_2_crack.png`……

### 批量导出（用 Abaqus Python 脚本）

在 Abaqus CAE 里：File → Run Script，新建一个 `.py` 文件，粘贴以下内容，
根据你的实际情况修改路径和切片编号：

```python
from abaqus import *
from abaqusConstants import *
import visualization
import os

# ===== 修改这里 =====
odb_path = r'C:\Users\PS\Desktop\crack_prediction\你的仿真文件.odb'
output_dir = r'C:\Users\PS\Desktop\crack_prediction\机器学习+裂纹预测\Abaqus_Plots_Clean\Porosity_XXXX\Crack_Plots'
slice_start = 1   # 切片起始编号
slice_end   = 40  # 切片结束编号
# ====================

os.makedirs(output_dir, exist_ok=True)

odb = visualization.openOdb(path=odb_path)
vp = session.viewports['Viewport: 1']
vp.setValues(displayedObject=odb)

# 选择 STATUSXFEM 场变量（如果用 Cohesive 就改成 SDEG）
vp.odbDisplay.setPrimaryVariable(
    variableLabel='STATUSXFEM',
    outputPosition=INTEGRATION_POINT
)

# 关掉图例和标题，保持图片干净
vp.viewportAnnotationOptions.setValues(
    triad=OFF, legend=OFF, title=OFF, state=OFF, annotations=OFF
)

for i in range(slice_start, slice_end + 1):
    # 这里需要根据你的模型切换截面，具体方法取决于你的模型设置
    # 如果是直接导出整体视图，去掉下面这行
    # session.viewports['Viewport: 1'].view.setValues(session=i)

    save_path = os.path.join(output_dir, f'slice_{i}_crack.png')
    session.printToFile(
        fileName=save_path,
        format=PNG,
        canvasObjects=(vp,)
    )
    print(f'已导出: slice_{i}_crack.png')

odb.close()
print('全部导出完成')
```

---

## 第三步：检查导出的图片

导出后打开几张对比一下：

1. 打开 `slice_1_geom.png` 和 `slice_1_crack.png`，叠在一起看
2. 裂纹位置应该在几何结构的孔隙附近，不应该出现在图片边缘或空白区域
3. 如果裂纹图全黑或全白，说明场变量选错了或者仿真没有跑出裂纹

---

## 第四步：放进数据目录

每个孔隙率文件夹下新建 `Crack_Plots/`，把对应的裂纹图放进去：

```
Abaqus_Plots_Clean/
├── Porosity_0.6749/
│   ├── Geom_Plots/
│   │   ├── slice_1_geom.png
│   │   └── ...
│   ├── Sener_Plots/
│   │   ├── slice_1_sener.png
│   │   └── ...
│   └── Crack_Plots/          ← 新建，放裂纹图
│       ├── slice_1_crack.png
│       └── ...
├── Porosity_0.6750/
│   └── ...（同上）
```

文件名必须对应：`slice_1_geom.png` ↔ `slice_1_sener.png` ↔ `slice_1_crack.png`

---

## 第五步：告诉我，我来改代码

Crack_Plots 放好之后来找我，我改以下 4 个地方（不超过 10 分钟）：

**改动 1**：`collect_pairs` 函数，扫描三元组（Geom + Sener + Crack）

**改动 2**：`CrackDataset`，加载第三张图（裂纹图）

**改动 3**：`train_step` 第 459 行，把伪目标换成真实裂纹：
```python
# 改前
target = sener_img
# 改后
target = crack_img
```

**改动 4**：`generate_images`，可视化加一列 Ground Truth，
变成 4 列：几何图 | 应变能密度 | 真实裂纹 | 预测裂纹

---

## 如果暂时没有裂纹仿真结果

可以先用应力图做阈值处理，生成粗糙的裂纹标注，跑通流程再换真实数据：

```python
from PIL import Image
import numpy as np
import os, glob

stress_dir = r'C:\Users\PS\Desktop\crack_prediction\机器学习+裂纹预测\Abaqus_Plots_Clean\Porosity_0.6749\Stress_Plots'
crack_dir  = stress_dir.replace('Stress_Plots', 'Crack_Plots')
os.makedirs(crack_dir, exist_ok=True)

for f in glob.glob(os.path.join(stress_dir, '*.png')):
    img = np.array(Image.open(f).convert('L'))
    # 取前 20% 高应力区域作为裂纹
    threshold = np.percentile(img, 80)
    crack = (img > threshold).astype(np.uint8) * 255
    # 转成 RGB 三通道（和 Geom/Sener 格式一致）
    crack_rgb = np.stack([crack, crack, crack], axis=-1)
    out_name = os.path.basename(f).replace('_stress.png', '_crack.png')
    Image.fromarray(crack_rgb).save(os.path.join(crack_dir, out_name))
    print(f'生成: {out_name}')

print('完成')
```

这个脚本直接在 `crack_prediction` 环境里跑就行：
```
"C:\Users\PS\miniconda3\envs\crack_prediction\python.exe" 上面这个脚本的路径.py
```

---

