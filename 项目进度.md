
## 怎么启动训练

### 第一步：打开终端

Win + R，输入 `cmd`，回车。或者直接打开 Anaconda Prompt。

### 第二步：激活环境

```
conda activate crack_prediction
```

### 第三步：运行训练脚本

```bash
# 推荐：v7 终极压榨版（RTX 3090 24G）— 纯张量缓存 + AMP + Batch64
python "C:\Users\PS\Desktop\crack_prediction\机器学习+裂纹预测\code for my project\crack_prediction_v7.py"
python "C:\Users\PS\Desktop\crack_prediction\机器学习+裂纹预测\code for my project\evaluate.py"
python "C:\Users\PS\Desktop\crack_prediction\机器学习+裂纹预测\code for my project\crack_prediction_two_stage.py"

crack_prediction_two_stage.py
# 备选版本（按场景选择）
# python crack_prediction_v6.py     # 256GB大内存：全量预加载到RAM
# python crack_prediction_v5.py     # 消除epoch间卡顿的v4升级版
# python crack_prediction_v4.py     # 3090高性能：Batch32 + 12 Workers
# python crack_prediction_v3.py     # 基础优化版：LSGAN + 自适应裁剪
# python crack_prediction_v2.2.py   # 修复内存暴涨 + AMP
# python crack_prediction_v1.py     # 最简版，适合验证
```
运行后你会看到：

```
[设备] 使用: cuda
[GPU] NVIDIA GeForce RTX 3090
[数据] 共找到 391 对 Geom-Sener 图片
[数据] 训练集: 312 对   测试集: 79 对
[模型] Generator 和 Discriminator 构建完成
[检查点] 未找到已有权重，从头开始训练
[训练] 开始...

========== Epoch 1/200 ==========
  Step    1 | G=...  L1=...  D=...
```

每个 epoch 大约 85 秒，200 个 epoch 总共约 5 小时。

### 训练输出在哪里

| 文件/文件夹 | 内容 |
|-------------|------|
| `outputs/` | 每个 epoch 的预测结果图，文件名 `pred_epoch0001_sample01.png` |
| `SaveModel/generator.pth` | 生成器权重，每个 epoch 自动保存 |
| `SaveModel/discriminator.pth` | 判别器权重 |
| `Loss.txt` | 每个 epoch 的 loss 记录 |

训练中断后重新运行，会自动从上次保存的 epoch 继续，不用从头来。

---

## 环境信息

| 项目 | 内容 |
|------|------|
| 环境名 | `crack_prediction` |
| 环境路径 | `C:\Users\PS\miniconda3\envs\crack_prediction\` |
| Python | 3.11.15 |
| PyTorch | 2.11.0+cu126 |
| CUDA | 12.6 |
| GPU | NVIDIA GeForce RTX 3090 |
| 数据路径 | `C:\Users\PS\Desktop\crack_prediction\机器学习+裂纹预测\Abaqus_Plots_Clean\` |

---

## 数据情况

数据目录：`Abaqus_Plots_Clean\`，里面有多个孔隙率文件夹，每个文件夹结构如下：

```
Porosity_0.6749/
├── Geom_Plots/      几何图（已有）
├── Sener_Plots/     应变能密度场（已有）
├── Stress_Plots/    应力场（已有，暂时没用上）
└── Crack_Plots/     裂纹路径图（待从 Abaqus 导出）
```

目前有效数据：391 对 Geom-Sener，训练集 312，测试集 79。
等 Crack_Plots 加进来，数量不变，但训练目标变成真实裂纹。

---

## 模型结构

- Generator：两路输入（Geom + Sener），8 层编解码器，全尺度跳跃连接
  每个解码层同时接收所有编码层的特征，捕捉多尺度裂纹信息
- Discriminator：PatchGAN，输入 Geom + Sener + 目标图（9 通道）
- 损失：GAN loss + L1 loss，L1 权重 LAMBDA=100

---

## 当前限制

代码第 459 行：

```python
target = sener_img   # 当前用 Sener 作伪目标；有真实裂纹标注后替换这里
```

这一行是占位符。现在模型学的是 Geom+Sener → Sener，没有实际意义。
等 Crack_Plots 准备好，把这行改成 `target = crack_img`，训练才真正有意义。

---

## 下一步

1. 在 Abaqus 里导出裂纹图 → 详见 `裂纹标注指南.md`
2. 把裂纹图放进对应的 `Crack_Plots/` 文件夹
3. 告诉我，我来改代码（改 4 个地方，10 分钟内搞定）
4. 重新启动训练

---

## 版本历史

详见 **[CHANGELOG.md](CHANGELOG.md)**

| 版本 | 日期 | 要点 |
|------|------|------|
| **v7** | 2026-05-11 | 纯张量缓存 + AMP + Batch64 + share_memory（3090终极版） |
| v6 | 2026-05-11 | 全量内存预加载（适配256GB大内存） |
| v5 | 2026-05-11 | persistent_workers 消除 Epoch 间卡顿 |
| v4 | 2026-05-11 | BATCH=32 + 12 Workers + OpenCV 线程控制 |
| **v3** | 2026-05-11 | LSGAN + 自适应裁剪 + 裂纹加粗 + 加权L1 + TTUR |
| v2.2 | 2026-05-10 | 修复内存暴涨（按需加载） |
| v2.1 | 2026-05-10 | AMP混合精度 + 断点续训 + 大批量 |
| v1 | 2026-05-10 | 基础 GAN + U-Net 架构 |


