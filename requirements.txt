# ============================================================
# 裂纹预测项目 - Python 依赖
# 适用于所有版本 (v1 ~ v7)
# ============================================================
# 安装命令: pip install -r requirements.txt
# ============================================================

# PyTorch (CUDA 12.6) — 见下方手动安装说明
# torch>=2.0.0
# torchvision>=0.15.0

# 核心依赖
numpy>=1.24.0
Pillow>=10.0.0
matplotlib>=3.7.0
tqdm>=4.65.0
opencv-python>=4.8.0

# 注意：PyTorch 不能通过 pip install -r requirements.txt 自动安装正确版本
# 必须根据 CUDA 版本手动安装，详见 SETUP_GUIDE.md
