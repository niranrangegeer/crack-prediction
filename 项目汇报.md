# 多孔发泡材料裂纹预测 — 基于 GAN 的端到端深度学习研究

## 一、研究背景与问题

多孔发泡材料在外力作用下会产生微裂纹，裂纹的萌生与扩展路径与材料内部的孔隙几何分布、应变能密度场密切相关。传统有限元方法（如 Abaqus）虽能模拟单个工况，但计算成本极高，难以用于快速预测和参数扫描。

本工作的核心思路是：将几何结构（Geom）与应变能密度场（Sener）作为输入，训练一个深度学习模型直接预测裂纹形态（Status），实现"输入结构 → 输出裂纹"的端到端映射，将预测时间从数小时缩短到毫秒级。

## 二、模型架构

采用 Pix2Pix（cGAN + U-Net）架构，包含两个网络：

- **生成器（Generator）：** 7 层编码器-解码器 U-Net，编码器逐步提取多尺度特征（从 256×256 压缩到 1×1），解码器通过全尺度跳跃连接融合浅层细节与深层语义。输入 6 通道（Geom 3通道 + Sener 3通道），输出 3 通道裂纹预测图。
- **判别器（Discriminator）：** PatchGAN 结构，4 层卷积，在 70×70 的感受野上判断真假，鼓励生成器输出局部纹理清晰的裂纹。

## 三、训练策略优化迭代

研究过程经历了 7 个版本迭代，每个版本针对特定瓶颈进行定向改进：

**1. 损失函数改进。** 将标准 GAN 的 BCE 损失替换为 Least Squares GAN（MSE 损失），解决了判别器过强时梯度饱和导致生成器停滞的问题；同时采用 Two Time-scale Update Rule，将判别器学习率从 2e-4 降低至 5e-5，使生成器有充足的学习空间。进一步引入加权 L1 Loss，对裂纹区域像素（归一化后值 < 0 的区域）赋予 50 倍权重，强制模型聚焦于稀疏的裂纹像素而非大量背景。

**2. 数据预处理增强。** Abaqus 导出的图像为长方形（含文字标签和留白），直接缩放至正方形会挤压孔洞形状。新增自适应居中裁剪模块，先裁剪最大中心正方形再缩放，从根本上消除了几何失真。同时引入形态学腐蚀操作将细裂纹线加粗，提高模型对微弱裂纹信号的感知能力。

**3. 系统性能优化。** 针对 RTX 3090 (24G) 和 76 核 CPU 的硬件配置，进行了系统的工程优化：关闭 OpenCV 内部多线程以防止与 DataLoader 多进程产生线程爆炸；启用 persistent_workers 消除 epoch 间 30 秒的进程重启开销；将数据预处理管线从 PIL 操作重构为纯 PyTorch Tensor 操作，并结合 share_memory_() 实现多进程零拷贝数据共享；重新引入 AMP 自动混合精度训练（FP16），将有效 batch size 提升至 64，GPU 利用率从 30% 提升至 90% 以上。

## 四、实验效果

在包含 391 组 Abaqus 仿真数据（312 训练 / 79 测试）的数据集上：

| 指标 | v1 初始版 | v7 最终版 |
|------|----------|----------|
| 单 epoch 耗时 | ~120 秒 | ~35 秒 |
| 800 epochs 总时长 | ~27 小时 | ~8 小时 |
| GPU 利用率 | 30-40% | 90-95% |
| Batch Size | 4 | 64 |

定性分析表明，加权 L1 Loss 和裂纹加粗使得模型能够捕捉到 v1 版本遗漏的细裂纹，LSGAN 损失使预测的裂纹边界更加清晰。自适应居中裁剪确保了预测结果的几何保真度。

## 五、后续工作

当前训练目标为 Abaqus 导出的 Status 图（应力状态标记）。下一阶段将替换为真实的裂纹路径标注图（Crack_Plots），完成从"应力状态预测"到"真实裂纹预测"的迁移。此外，可探索引入物理约束损失项（如能量守恒约束），进一步提高预测的物理一致性。在工程化方面，可将训练好的模型封装为独立推理模块，集成至 Abaqus 后处理流程，实现"一键出裂纹预测"。

---

> 代码仓库：https://github.com/niranrangegeer/crack-prediction
> 完整技术演进日志：[CHANGELOG.md](CHANGELOG.md)
> 新环境搭建：[SETUP.md](SETUP.md)
